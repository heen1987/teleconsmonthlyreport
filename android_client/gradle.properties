android.dependency.useConstraints=false
android.overridePathCheck=true
android.uniquePackageNames=false
android.useAndroidX=true

# AI-PMS Server Connections
# Public/release builds must inject URLs through Gradle properties or env vars:
#   -PaipmsPlatformBaseUrl=https://<platform-server-url>
#   -PaipmsCollectionBaseUrl=https://<collection-public-url>
# Do not commit temporary trycloudflare URLs here.
